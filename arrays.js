var lista = ['pera', 'Uva', 'Jaca', 'Mamão']

var cont = 0
while (cont < lista.length){
    console.log(lista[cont])
    cont++
}

for (let cont = 0; cont < 4; cont++){
    console.log(lista[cont])
}
