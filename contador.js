const Promp = require("prompt-sync")()

const prompt = require(PromptSync)
var cont = 0

while (cont < 10) { // ENQUANTO
    cont++ // cont = cont + 1
    console.log(cont)
}

var cont2 = 10

while (cont2 > 0) { // ENQUANTO
    cont2-- // cont = cont - 1
    console.log(cont2)
}